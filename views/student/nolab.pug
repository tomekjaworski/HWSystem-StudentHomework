extends ../includes/layout


block content
    .row
        .col-lg-12
            div.d-flex.justify-content-center
                .col-8
                    .card
                        .card-block
                            if lab
                                h2=i18n('student.nolab.inactive.%s', lab.labgroup.name)
                                //h2
                            else
                                h2=i18n('student.nolab')
                                //h2
