extends ../includes/layout

mixin entry(id)
    tr
        th(scope='row')= id
        td Mark
        td Otto
        td @mdo

block content
    .row
        .col-md-12.col-lg-4
            include topics
        .hidden-md-down.col-lg-8
            include tasks