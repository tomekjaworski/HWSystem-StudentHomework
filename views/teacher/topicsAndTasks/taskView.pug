extends ../../includes/layout

block content

    .row
        .col-md-12.col-lg-4
            include ../menu

        .col-md-12.col-lg-8
            h1= task.title
            h2= task.topic.number + task.topic.title
            if description
                div!=description
            else
                p nie ma opisu zadania